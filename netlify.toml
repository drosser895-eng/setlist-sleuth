[build]
  command = "npm install && npm run build"
  publish = "verifier-ui/dist"

[context.production.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
